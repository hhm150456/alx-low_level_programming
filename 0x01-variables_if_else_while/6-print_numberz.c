#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: A C program that prints the base 10 numbers
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	for (int i = 48; i < 58; ++i)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
