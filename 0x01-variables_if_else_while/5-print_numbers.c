#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: A C program that prints the base 10 numbers
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	for (int i = 0; i < 10; ++i)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
